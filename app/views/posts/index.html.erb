<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<!--<table class= "table table-condensed table-bordered table-hover ">
  <thead class="head">
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= truncate(post.body) {link_to "see more", post}%></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<div class="Jumbotron">
<div class="container">
  <% @posts.each do |post| %>
<h2> <%= post.title %></h2>
<p><%= truncate(post.body, length:520) %></p>
<%= link_to '<button class="btn btn-default" role="button">See more</button>'.html_safe, post%>
<%= link_to '<button class="btn btn-default" role="button">Edit</button>'.html_safe, edit_post_path(post) %>
<%= link_to '<button class="btn btn-default" role="button">Destroy</button>'.html_safe, post, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
</div> 
  </div>

<br>

<%= link_to '<button class="btn btn-default" role="button">New Post</button>'.html_safe, new_post_path %>
